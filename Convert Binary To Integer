int BinaryToInteger(string str){
    int d = 1,ans=0;
    int len = str.length();
    for(int i = len-1 ; i >= 0 ; i--){
        if(str[i]=='1'){
            ans+=d;
        }
        d*=2;
    }
    return ans;
}
